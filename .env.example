# Hugging Face — set this if you need access to gated models (PaliGemma, etc.).
HF_TOKEN=

# Where to cache model + dataset downloads. Mounted into the container as /data.
HF_HOME=./data/.hf_cache

# Optional: Weights & Biases for experiment tracking.
WANDB_API_KEY=
WANDB_PROJECT=ais5
