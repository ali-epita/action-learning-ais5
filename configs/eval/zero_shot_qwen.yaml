# Zero-shot Qwen2.5-VL-3B on ScreenSpot-V2.
# Used as the team's baseline / smoke test before any LoRA work.
seed: 42
limit: null  # set to e.g. 50 for a quick smoke run

model:
  name: qwen2.5-vl-3b
  kwargs:
    device_map: auto
    torch_dtype: auto
    max_new_tokens: 64

benchmark: screenspot-v2
data: {}
